depends gettext
depends gtk+-3
depends itstool
depends yelp-tools

optional_depends libnotify4 \
                 "--enable-notify" \
                 "--disable-notify" \
                 "for libnotify support"

